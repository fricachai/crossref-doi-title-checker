# Crossref DOI Title Checker

This script checks the consistency between DOIs and article titles
using the Crossref REST API.

## Usage
1. Edit the `dois` list in `app.py`
2. Run:
```bash
python app.py
